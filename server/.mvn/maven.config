# Applied to every `./mvnw` in server/ (CI, the Dockerfile and this Mac), so no caller has to repeat them.
# One argument per line: Maven splits this file on whitespace.
--batch-mode
--no-transfer-progress
-T1C
