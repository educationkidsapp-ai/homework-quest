/* Playwright specs — Node + Playwright types only, no Angular compilation. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "noEmit": true
  },
  "include": ["e2e/**/*.ts", "playwright.config.ts"]
}
