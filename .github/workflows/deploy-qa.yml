name: Deploy QA

# develop → QA project. Builds the image once, tags it with the commit SHA (production later promotes that exact digest),
# applies Terraform, deploys Cloud Run, the admin panel and a QA APK, and posts the links on the merged PR.
on:
  push:
    branches: [develop]
  workflow_dispatch:

concurrency:
  group: deploy-qa
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write
  pull-requests: write
  actions: read

env:
  TAG: ${{ github.sha }}

jobs:
  image:
    name: Build image
    runs-on: ubuntu-latest
    environment: qa
    outputs:
      digest: ${{ steps.push.outputs.digest }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/gcp-auth
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}
          region: ${{ vars.GCP_REGION }}
          wif_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOYER_SA }}
      - uses: docker/setup-buildx-action@v3
      - name: Build and push the image (Gradle contract → Maven → Temurin 21 + LibreOffice)
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ vars.IMAGE }}:${{ env.TAG }}
            ${{ vars.IMAGE }}:develop
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: APP_VERSION=${{ env.TAG }}

  infra:
    name: Terraform apply (QA)
    needs: image
    runs-on: ubuntu-latest
    environment: qa
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/gcp-auth
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}
          region: ${{ vars.GCP_REGION }}
          wif_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOYER_SA }}
      - uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }
      - working-directory: infra/terraform
        env:
          TF_VAR_deepseek_api_key: ${{ secrets.DEEPSEEK_API_KEY }}
          TF_VAR_admin_password: ${{ secrets.ADMIN_PASSWORD }}
          TF_VAR_firebase_credentials_json: ${{ secrets.FIREBASE_CREDENTIALS }}
        run: |
          terraform init -backend-config=envs/qa.backend -input=false
          terraform apply -var-file=envs/qa.tfvars -auto-approve -input=false

  deploy:
    name: Cloud Run + migrations
    needs: [image, infra]
    runs-on: ubuntu-latest
    environment:
      name: qa
      url: ${{ vars.API_URL }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/gcp-auth
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}
          region: ${{ vars.GCP_REGION }}
          wif_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOYER_SA }}
      - name: Deploy (Flyway migrates on start; the startup probe waits for /health)
        run: |
          gcloud run deploy homework-quest-api --region "${{ vars.GCP_REGION }}" \
            --image "${{ vars.IMAGE }}@${{ needs.image.outputs.digest }}" \
            --update-env-vars "APP_VERSION=${TAG:0:7}" --quiet
      - name: Smoke test
        run: |
          for _ in $(seq 1 20); do curl -fsS "${{ vars.API_URL }}/health" && exit 0; sleep 6; done; exit 1

  admin:
    name: Admin panel → Firebase Hosting (QA)
    needs: deploy
    runs-on: ubuntu-latest
    environment: qa
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: "17" }
      - uses: gradle/actions/setup-gradle@v4
      - run: ./gradlew :webAdmin:wasmJsBrowserDistribution -Pquest.admin.apiBaseUrl=${{ vars.API_URL }} --no-daemon
      - uses: ./.github/actions/gcp-auth
        with:
          project_id: ${{ vars.GCP_PROJECT_ID }}
          region: ${{ vars.GCP_REGION }}
          wif_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_DEPLOYER_SA }}
      - name: firebase deploy (auth through the WIF-issued ADC)
        run: npx --yes firebase-tools@14 deploy --only hosting:admin --project "${{ vars.GCP_PROJECT_ID }}" --non-interactive

  apk:
    name: QA APK
    needs: deploy
    runs-on: ubuntu-latest
    environment: qa
    outputs:
      artifact_url: ${{ steps.upload.outputs.artifact-url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: "17" }
      - uses: gradle/actions/setup-gradle@v4
      - name: Signing key (optional — debug key when the secret is absent)
        env:
          KEYSTORE_B64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_B64" ]; then echo "$KEYSTORE_B64" | base64 -d > "$RUNNER_TEMP/release.jks"; echo "ANDROID_KEYSTORE_FILE=$RUNNER_TEMP/release.jks" >> "$GITHUB_ENV"; fi
      - run: ./gradlew :androidApp:assembleQaRelease -Pquest.qa.apiBaseUrl=${{ vars.API_URL }} --no-daemon
        env:
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
      - uses: actions/upload-artifact@v4
        id: upload
        with:
          name: homework-quest-qa-${{ github.sha }}
          path: androidApp/build/outputs/apk/qa/release/*.apk
          retention-days: 30

  announce:
    name: Post links on the merged PR
    needs: [image, deploy, admin, apk]
    if: always() && needs.deploy.result == 'success'
    runs-on: ubuntu-latest
    environment: qa
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const sha = context.sha;
            const prs = await github.rest.repos.listPullRequestsAssociatedWithCommit({ ...context.repo, commit_sha: sha });
            const body = [
              `🚀 **Deployed to QA** (\`${sha.slice(0, 7)}\`)`,
              `- API: ${{ vars.API_URL }}`,
              `- Admin panel: ${{ vars.HOSTING_URL }}`,
              `- QA APK: ${{ needs.apk.outputs.artifact_url || 'build failed' }}`,
              `- Image digest for promotion: \`${{ needs.image.outputs.digest }}\``,
            ].join('\n');
            for (const pr of prs.data) await github.rest.issues.createComment({ ...context.repo, issue_number: pr.number, body });
            if (!prs.data.length) core.notice(body);
